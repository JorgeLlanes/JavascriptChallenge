// 3)

// MEDIUM: Write a program to tell if someone is shouting (typing in all caps), whispering (typing in all lowercase), 
// or neither. Use prompt to get user input, 
// and then console log whether the user was shouting, whispering, or talking normally.

let tone = prompt("Where is the turtle", "Outside");

if (tone === tone.toUpperCase()) {

    console.log("User is Shouting")

} else if (tone === tone.toLowerCase()) {

    console.log("User is whispering")
}

else (tone); {

    console.log("User is speaking normally")
};


// javaScript is link to html file : index.html ... inside body //